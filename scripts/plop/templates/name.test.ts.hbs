import { expect, fixture } from "@open-wc/testing";
import { html } from "lit";
import { type I{{properCase name}} } from "./{{name}}";

describe("I{{properCase name}}", () => {
  it("default values are set correctly", async () => {
    const el = await fixture<I{{properCase name}}>(html`<i-{{name}}>default</i-{{name}}>`);

    expect(el.example).to.equal("");
  });
});
